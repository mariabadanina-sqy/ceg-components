import * as React from 'react';
import { Box } from '@sqymagma/elements';
import MasterTemplate from '../MasterTemplate';

const Sitemap = () => {
  return (
    <MasterTemplate>
      <Box>sitemap template</Box>
    </MasterTemplate>
  );
};

export default Sitemap;
